# HA Device Summary — Beispiele (nach HACS-Installation)

# Standard: Stockwerk → Räume, Badges, „offen“ = on
type: custom:ha-device-summary

# --- Nur Stockwerk (alle Geräte des Stockwerks als Badges) ---
# type: custom:ha-device-summary
# group_by: floor
# show_devices: true

# --- Nur Raum (flache Liste nach Bereich) ---
# type: custom:ha-device-summary
# group_by: area
# show_devices: true

# --- Nur Zahlen, ohne Badges ---
# type: custom:ha-device-summary
# group_by: both
# show_devices: false

# --- Fenster + Türen, längere Badge-Namen ---
# type: custom:ha-device-summary
# title: Fenster & Türen
# device_classes:
#   - window
#   - door
# truncate_entity: 28
