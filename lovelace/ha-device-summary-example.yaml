# HA Device Summary — Beispiel

type: custom:ha-device-summary
title: EG + Zwischenbau
floor_ids:
  - erdgeschoss
  - zwischenbau
entity_domain: binary_sensor
device_classes: [window]
active_states: ["on"]
use_native_rows: true
